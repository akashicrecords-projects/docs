# AKChain EIP-7702 协议升级实施方案

## 1. 概述

本文用于说明 AKChain 网络进行 **EIP-7702 协议升级**的实施流程，包括升级前备份、升级投票、网络快照、节点程序切换、网络恢复，以及升级后新节点的加入方式。

本次升级采用**指定区块高度硬分叉/协议升级**方式实施。所有验证节点及观察节点需要在约定的升级高度完成旧版本节点到支持 EIP-7702 的新版本节点切换。

为保证升级过程中链状态一致、出现异常时能够快速恢复，升级前必须完成完整的数据备份，并明确记录：

- 备份高度
- 升级高度
- 启动快照高度
- 新节点创世配置中的启动高度

---

# 2. 升级总体流程

本次升级按照以下顺序执行：

```text
升级准备
   │
   ├── 1. 选择观察节点进行备份验证
   │
   ├── 2. 停止节点并确认备份高度
   │
   ├── 3. 启动节点至指定高度并自动停止
   │
   ├── 4. 完成全量数据备份 / 云主机快照
   │
   ├── 5. 恢复节点正常运行
   │
   ▼
升级投票
   │
   ├── 发起 AKChain 升级提案
   ├── 验证节点进行投票
   └── 达到治理权重门槛后升级提案通过
   │
   ▼
协议升级
   │
   ├── 网络运行至升级高度
   ├── 旧版本节点停止
   └── 所有节点切换至 EIP-7702 新版本
   │
   ▼
设置启动快照
   │
   └── 官方验证节点设置启动快照
   │
   ▼
网络恢复
   │
   ├── 依次启动验证节点
   └── 依次启动观察节点
   │
   ▼
升级完成
   │
   └── 验证 EIP-7702 功能及网络状态
```

---

# 3. 升级前准备

## 3.1 确认升级版本

在正式升级前，应确认以下内容：

- 支持 EIP-7702 的 AKChain 新版本节点软件已经发布。
- 所有验证节点均可以获取新版本软件。
- 所有观察节点均可以获取新版本软件。
- 新旧版本节点的兼容性已经完成测试。
- EIP-7702 相关交易、执行逻辑及 RPC 接口已经完成测试。
- 升级高度已经确定。
- 升级快照高度已经确定。
- 新节点加入网络所需的创世文件已经准备完成。
- 升级操作手册及回滚方案已经准备完成。

---

# 4. 升级前数据备份

## 4.1 选择备份节点

选择一个观察节点作为升级前状态备份节点。

建议优先选择：

- 数据完整；
- 与主网络同步正常；
- 磁盘空间充足；
- 网络稳定；
- 不承担验证节点共识职责。

该节点主要用于确认升级前的链状态，并作为升级异常情况下的重要恢复依据。

---

## 4.2 停止观察节点

首先停止该观察节点当前运行的节点程序。

确认节点已经完全停止，并确认没有正在进行的数据写入操作。

---

## 4.3 按指定高度启动节点

再次启动节点程序，但增加**指定区块高度停止参数**。

该高度应设置为本次升级前需要备份的区块高度：

```text
BACKUP_HEIGHT = xxx
```

节点运行至 `BACKUP_HEIGHT` 后自动停止。

此时节点的数据状态即对应升级前确定的备份高度。

---

## 4.4 执行全量备份

确认节点已经在 `BACKUP_HEIGHT` 停止后，执行完整数据备份。

备份方式可以采用：

1. 全量复制节点 `data` 目录；
2. 云主机磁盘快照；
3. 其他经过验证的数据备份机制。

建议至少保留一份**独立于节点运行环境的完整备份**。

同时记录以下信息：

```text
备份高度：xxx
备份时间：YYYY-MM-DD HH:mm:ss
节点版本：xxx
数据目录：xxx
备份方式：全量数据 / 云主机快照
备份位置：xxx
```

---

## 4.5 恢复节点正常运行

备份完成后，恢复该观察节点正常运行。

此时：

- 删除或取消指定区块高度停止参数；
- 使用原有正常启动参数启动节点；
- 确认节点可以继续同步；
- 确认节点状态正常。

---

# 5. 升级治理投票

## 5.1 发起升级提案

按照 AKChain 治理机制发起 EIP-7702 协议升级提案。

提案至少应包含：

- 升级名称：EIP-7702
- 升级版本：xxx
- 升级高度：`UPGRADE_HEIGHT`
- 新节点软件版本：xxx
- 升级生效规则
- 升级实施时间
- 升级风险及回滚方案

---

## 5.2 验证节点投票

由 AKChain 验证节点进行升级投票。

当赞成票达到治理机制规定的**验证节点权重门槛**后，升级提案通过。

只有升级提案通过后，才能进入正式升级阶段。

---

# 6. 网络升级

## 6.1 确认升级高度

正式升级前，再次确认：

```text
UPGRADE_HEIGHT = yyy
```

所有节点必须以该高度作为本次协议升级的统一边界。

---

## 6.2 网络运行至升级高度

旧版本 AKChain 节点继续运行。

当区块高度达到：

```text
yyy
```

旧版本协议执行至升级边界。

此时按照升级方案停止旧版本节点程序。

---

## 6.3 替换节点程序

旧版本节点停止后，将节点程序替换为支持 EIP-7702 的新版本。

所有验证节点和观察节点均需要完成软件升级。

节点配置中应确保：

- 网络 ID 不变；
- 节点身份不变；
- 验证节点密钥不变；
- 数据目录正确；
- P2P 配置正确；
- RPC 配置正确；
- EIP-7702 新版本配置正确。

---

# 7. 设置启动快照

## 7.1 选择官方验证节点

选择一个官方验证节点作为**启动快照节点**。

该节点用于生成升级后的网络启动状态。

---

## 7.2 设置启动快照

在该官方验证节点上配置启动快照。

例如：

```text
UPGRADE_HEIGHT = yyy

SNAPSHOT_HEIGHT = yyy + 10
```

启动快照的目的，是在协议升级完成并经过若干区块确认后，以一个确定且稳定的链状态作为后续节点加入网络的基准状态。

---

## 7.3 快照状态确认

生成启动快照后，应记录：

```text
快照高度：yyy + 10
快照哈希：xxx
链状态信息：xxx
节点版本：xxx
生成时间：YYYY-MM-DD HH:mm:ss
```

同时应验证快照对应的链状态能够正常启动新版本节点。

---

# 8. 启动升级后的网络

## 8.1 启动验证节点

确认所有验证节点已经完成新版本软件替换后，按照预定顺序依次启动验证节点。

建议不要一次性同时启动所有验证节点，而采用：

```text
验证节点 1
    ↓
确认同步及共识正常
    ↓
验证节点 2
    ↓
确认同步及共识正常
    ↓
...
    ↓
所有验证节点启动完成
```

每启动一个验证节点后，应确认：

- 节点进程正常；
- P2P 连接正常；
- 区块高度正常增长；
- 共识状态正常；
- 验证节点正常参与出块/共识；
- EIP-7702 相关交易执行正常。

---

## 8.2 启动观察节点

所有验证节点恢复正常后，再依次启动观察节点。

观察节点启动后确认：

- 可以连接验证节点；
- 可以正常同步区块；
- 区块高度与网络一致；
- RPC 服务正常；
- EIP-7702 相关交易查询正常。

---

# 9. 升级完成验证

升级完成后，应进行全面验证。

## 9.1 区块状态验证

确认：

```text
当前区块高度 > SNAPSHOT_HEIGHT
```

并确认所有节点区块高度一致。

---

## 9.2 共识状态验证

确认：

- 验证节点数量正常；
- 验证节点权重正常；
- 共识正常；
- 区块持续产生；
- 无持续性共识异常；
- 无节点反复退出或重启。

---

## 9.3 EIP-7702 功能验证

至少进行以下验证：

- EIP-7702 交易可以正常提交；
- 交易可以正常打包；
- 交易执行结果正确；
- Gas 计算正确；
- 状态变更正确；
- RPC 查询结果正确；
- 相关账户授权状态正确；
- 升级前原有交易类型仍然正常；
- 升级前已有账户及资产状态保持一致。

---

## 9.4 数据一致性验证

对多个验证节点和观察节点进行状态比对，确认：

```text
Block Height
Block Hash
State Root
Transaction Root
Receipt Root
```

等关键链状态信息一致。

---

# 10. 新增验证节点或观察节点

EIP-7702 升级完成后，新加入 AKChain 网络的节点必须使用支持 EIP-7702 的节点软件。

## 10.1 下载新版本节点软件

下载 AKChain 官方发布的、支持 EIP-7702 的节点软件。

禁止使用不支持 EIP-7702 的旧版本节点程序加入升级后的网络。

---

## 10.2 获取新的创世配置

使用升级后的网络创世配置。

创世文件中需要根据当前网络状态设置启动高度。

例如：

```text
SNAPSHOT_HEIGHT = yyy + 10

NEW_NODE_START_HEIGHT = yyy + 20
```

新节点应从指定的启动快照及其后的网络状态开始同步。

---

## 10.3 新节点启动

完成以下配置后启动节点：

- 新版本节点软件；
- 正确的 Chain ID；
- 正确的 Network ID；
- 正确的 P2P 配置；
- 正确的 Bootnodes / Seed Nodes；
- 正确的创世文件；
- 正确的启动快照；
- 正确的数据目录。

启动后确认节点能够正常连接网络并完成同步。

---

# 11. 回滚方案

升级过程中如果出现严重异常，应优先停止继续扩大升级范围，并根据异常情况决定是否执行回滚。

## 11.1 回滚触发条件

出现以下情况之一时，可以考虑启动回滚流程：

- 共识无法正常进行；
- 区块无法持续产生；
- 多数验证节点无法正常启动；
- 新版本节点出现严重状态异常；
- EIP-7702 执行结果与预期不一致；
- 出现严重的数据一致性问题。

---

## 11.2 数据恢复

如果需要恢复升级前状态，可以使用第 4 节保存的备份数据。

恢复时应明确：

```text
恢复高度 = BACKUP_HEIGHT
```

并按照 AKChain 官方回滚方案恢复节点网络。

---

# 12. 升级操作检查清单

## 12.1 升级前

- [ ] EIP-7702 新版本节点软件完成测试
- [ ] 所有验证节点完成升级准备
- [ ] 所有观察节点完成升级准备
- [ ] 升级提案已经提交
- [ ] 升级投票已经通过
- [ ] 升级高度已经确定
- [ ] 备份节点已经选择
- [ ] 备份数据已经完成
- [ ] 备份高度已经记录
- [ ] 回滚方案已经确认

## 12.2 升级执行

- [ ] 网络运行至升级高度
- [ ] 旧版本节点停止
- [ ] 验证节点程序全部替换
- [ ] 观察节点程序全部替换
- [ ] 启动快照已经设置
- [ ] 快照高度已经记录
- [ ] 快照状态验证通过
- [ ] 验证节点依次启动
- [ ] 验证节点共识恢复正常
- [ ] 观察节点依次启动
- [ ] 所有节点同步正常

## 12.3 升级后

- [ ] 区块高度持续增长
- [ ] 所有验证节点状态正常
- [ ] 所有观察节点同步正常
- [ ] Block Hash 一致
- [ ] State Root 一致
- [ ] EIP-7702 交易测试通过
- [ ] 原有交易功能测试通过
- [ ] RPC 接口测试通过
- [ ] 新节点加入测试通过
- [ ] 升级结果正式确认

---

# 13. 2026 年 08 月 EIP-7702 升级参数

| 参数 | 配置 |
|---|---|
| 升级版本 | EIP-7702 |
| 备份高度 | `xxx` |
| 升级高度 | `yyy` |
| 快照高度 | `yyy + 10` |
| 新节点启动高度 | `yyy + 20` |
| 新节点软件版本 | `xxx` |
| 升级提案 | `xxx` |
| 投票权重门槛 | `xxx` |
| 快照节点 | `xxx` |
| 备份节点 | `xxx` |

---

# 14. 关键高度关系

本次升级涉及的几个关键高度必须严格区分：

```text
BACKUP_HEIGHT
      │
      │  升级前数据备份
      ▼
UPGRADE_HEIGHT
      │
      │  EIP-7702 协议正式生效
      ▼
SNAPSHOT_HEIGHT
      │
      │  升级后网络稳定状态
      ▼
NEW_NODE_START_HEIGHT
      │
      │  新节点加入网络
      ▼
正常运行
```

本次示例参数为：

```text
备份高度       = xxx
升级高度       = yyy
快照高度       = yyy + 10
新节点启动高度 = yyy + 20
```

上述高度应在正式升级前由 AKChain 网络治理及技术团队最终确认，并在升级公告、节点配置及操作记录中保持一致。